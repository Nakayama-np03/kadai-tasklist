<h1>id: <%= @task.id %>のタスクを編集</h1>

    <%= form_with(model: @task, local: true) do |f| %>
        <%= f.label :content, 'タスク名' %>
        <%= f.text_field :content %>
        
        <%= f.submit '変更' %>
    <% end %>
    
<%= link_to 'タスク一覧',tasks_path %>